services:
  color-api:
    build: .
    ports:
      - "8001:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - CAMERA_URL_FLOOR=${CAMERA_URL_FLOOR}
      - CAMERA_URL_WOOD=${CAMERA_URL_WOOD}
    restart: unless-stopped
